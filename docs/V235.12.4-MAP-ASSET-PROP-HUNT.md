# ATM Town v235.12.4 — Map-Asset Prop Hunt

## Summary

This build upgrades the Prop Hunt world event so disguised players use **real ATM Town map-item artwork** instead of generic placeholder prop drawings.

## Included prop disguises

The current Prop Hunt disguise pool uses cropped artwork from actual town objects:
- arcade ATM machine
- bench
- ATM Town News board
- street lamp
- Character Upgrades board

These assets live in:
- `assets/world-events/prop-hunt/atm_machine.png`
- `assets/world-events/prop-hunt/bench.png`
- `assets/world-events/prop-hunt/news_board.png`
- `assets/world-events/prop-hunt/street_lamp.png`
- `assets/world-events/prop-hunt/upgrade_board.png`

## Client updates

Files touched:
- `js/prop-hunt.js`
- `js/world-events.js`
- `js/runtime/game-core.js`
- `service-worker.js`
- `js/config.js`
- `README.md`

Changes:
- replaced placeholder prop rendering with image-based rendering
- preloads prop images from `assets/world-events/prop-hunt/`
- keeps existing server-synchronized Prop Hunt roles, countdown, hide phase, hunt phase, and tag flow
- keeps the normal ACTION button as the hunter tag action
- precaches the Prop Hunt prop assets in the service worker

## Server updates

File touched:
- `lib/world-events.js`

Change:
- updated the Prop Hunt assignment pool so the server now assigns the real map-prop ids instead of the placeholder ids

## Testing checklist

1. Start Prop Hunt with at least 2 online players.
2. Confirm the hunter still remains a normal character.
3. Confirm all non-hunter players become one of the real ATM Town map props listed above.
4. Confirm both local and remote clients see the same disguised prop identity for each player.
5. Confirm the hunter still gets the `TAG` action when near a disguised prop.
6. Confirm the last hidden prop still wins and the winner display still appears.
7. Confirm installed/PWA clients refresh correctly after the cache bump.

## Deployment

- No SQL is required.
- Run the normal build validator.
